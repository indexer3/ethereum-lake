package event

import "testing"

func TestOnEventHash(t *testing.T) {
	t.Run("test on transfer event hash", func(t *testing.T) {

	})

	t.Run("test on approval event hash", func(t *testing.T) {

	})

	t.Run("test on transfer batch event hash", func(t *testing.T) {

	})
}
